(jbuild_version 1)

(executable
 ((name    tests)
  (modules (tests))
  (libraries (interval))))

(executable
 ((name      test_base)
  (modules   (test_base))
  (libraries (interval_base))))

(alias
 ((name runtest)
  (package interval_base)
  (deps (test_base.exe))
  (action (run ${<}))))
